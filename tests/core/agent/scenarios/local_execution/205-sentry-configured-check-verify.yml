id: 205-sentry-configured-check-verify
title: Sentry configured check runs verifier
intent_class: local_execution
input:
  prompt: Is Sentry configured currently?
session:
  has_prior_state: false
  configured_integrations: []
  resolved_integrations: {}
available_capabilities:
  slash_commands:
  - /integrations
  cli_commands: []
  synthetic_suites: []
notes:
- "Regression for #3103: current-state configuration questions should execute the verifier, not suggest a CLI command."
turn:
  expected_kind: agent
policy:
  executes_terminal_action: true
planned_actions:
- kind: slash
  content: /integrations verify sentry
  source: llm
  target_surface: slash
  command: /integrations
  args:
  - verify
  - sentry
response_contract:
  must_contain_any:
  - ran /integrations verify sentry
  must_not_contain: []
history:
  expected:
  - type: slash
    ok: true
runs: 1
tool_actions:
- surface: dispatch
  kind: slash
  command: /integrations
  args:
  - verify
  - sentry
  content: /integrations verify sentry
